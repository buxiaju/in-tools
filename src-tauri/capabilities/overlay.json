{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "框选覆盖层窗口的权限：调用工具 + 截图前自隐藏 + 关闭自身。",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
