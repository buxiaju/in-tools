# 网络工具

Ping、DNS 查询、端口检测、HTTP 请求、本机 IP 获取。

## 工具

| 工具 | 说明 |
|------|------|
| `net:ping` | Ping 指定主机 |
| `net:dns` | 查询域名的 DNS A 记录 |
| `net:check-port` | 检测主机端口是否开放 |
| `net:http` | 发送 HTTP GET 请求 |
| `net:ip` | 获取本机局域网和公网 IP |
